import { StyleSheet } from 'react-native';

const historyDetail_styles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        backgroundColor: '#FFFFFF',
    },
});

export default historyDetail_styles;