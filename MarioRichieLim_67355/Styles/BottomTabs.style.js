import { StyleSheet } from 'react-native';

const bottomTabs_styles = StyleSheet.create({
    qrisContainer: {
        top: -15,
        borderRadius: 15,
        borderWidth: 5, 
        borderColor: '#FFF',  
    },
    qris: {
        width: 50,
        height: 50,
        borderRadius: 10,
    },
    normal: {
        width: 24,
        height: 24,
    },
});

export default bottomTabs_styles;
